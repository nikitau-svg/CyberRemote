<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Required for mDNS discovery: without a MulticastLock, NsdManager
         silently finds nothing on many devices. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Voice input (dictation into TV search boxes). Optional: requested only
         when the user taps the mic; on-device recognition is preferred. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Button-press vibration feedback (normal permission, auto-granted). -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- User-enabled controls in the notification shade. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!-- Voice input is optional, so don't gate installation on having a mic. -->
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!-- Lets us resolve the system speech-recognition service on Android 11+. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:label="CyberRemote QS"
        android:icon="@mipmap/ic_launcher"
        android:theme="@android:style/Theme.Material.NoActionBar"
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".quick.RemotePanelActivity"
            android:theme="@style/Theme.CyberRemote.RemotePanel"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:finishOnTaskLaunch="true" />

        <service
            android:name=".quick.RemoteControlService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <service
            android:name=".quick.RemoteTileService"
            android:exported="true"
            android:icon="@drawable/ic_qs_remote"
            android:label="Apple TV"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.ACTIVE_TILE"
                android:value="true" />
        </service>
    </application>
</manifest>
